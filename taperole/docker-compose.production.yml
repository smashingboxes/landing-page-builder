# Production Docker Setup Node
# Redis
redis:
  image: redis

# Postgres
# db:
#   image: postgres

web:
  build: .
  command: npm start
  environment:
    REDIS_HOST: redis
  links:
    - redis
  ports:
    - "80:3000"
